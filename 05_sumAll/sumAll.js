const sumAll = function(num1,num2) {
    for(let i = 0; i < num2; i++){
        let nextNum = arr[arr.length - 1] += 1;
        arr.push(nextNum);
    }
};

// Do not edit below this line
module.exports = sumAll;
